{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "Example test automation",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules",
    "reinstall": "npm run clean && npm install",
    "test": "jest",
    "test.watch": "jest --watchAll"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.31",
    "jest": "^26.6.3",
    "jest-playwright-preset": "^1.4.7",
    "playwright": "^1.9.1",
    "ts-jest": "^26.5.3",
    "typescript": "^4.2.3"
  }
}
